---
phase: 01-identity-and-coaching-relationships
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - supabase/migrations/20260309_phase1_invites.sql
  - src/features/invites/schemas.ts
  - src/features/invites/service.ts
  - src/app/api/invites/send/route.ts
  - src/app/api/invites/resend/route.ts
  - src/app/invite/[token]/page.tsx
  - src/app/api/invites/accept/route.ts
  - src/services/email/invite-template.ts
autonomous: true
requirements:
  - RELA-01
  - RELA-02
user_setup: []
must_haves:
  truths:
    - "Trainer can send invite to client email from app and resend without creating duplicate active invites."
    - "Invite links expire after 7 days and expired links cannot activate relationships."
    - "Client can accept a valid invite through guided sign-in/sign-up flow and become linked to exactly one trainer."
  artifacts:
    - path: src/features/invites/service.ts
      provides: "Invite issuance, resend refresh, and acceptance transition logic"
      min_lines: 60
    - path: src/app/api/invites/send/route.ts
      provides: "Authenticated trainer invite-send endpoint"
      exports: ["POST"]
    - path: src/app/api/invites/accept/route.ts
      provides: "Token validation and invite acceptance endpoint"
      exports: ["POST"]
    - path: supabase/migrations/20260309_phase1_invites.sql
      provides: "Invite token/expiry persistence model and constraints"
      min_lines: 25
  key_links:
    - from: src/app/invite/[token]/page.tsx
      to: src/app/api/invites/accept/route.ts
      via: "accept invite submit"
      pattern: "invite|accept"
    - from: src/app/api/invites/send/route.ts
      to: src/services/email/invite-template.ts
      via: "invite email dispatch"
      pattern: "email|send"
    - from: src/features/invites/service.ts
      to: supabase/migrations/20260309_phase1_invites.sql
      via: "token and expiry storage/query"
      pattern: "expires|token|invited|active"
---

<objective>
Implement invite lifecycle and relationship activation flow with expiry and idempotent resend semantics.

Purpose: Deliver trainer-client linking behavior required by Phase 1 without introducing duplicate relationship states.
Output: Invite send/resend/accept APIs, guided invite route, and persisted tokenized invite lifecycle.
</objective>

<execution_context>
@C:/Users/Carboteiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Carboteiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-and-coaching-relationships/01-CONTEXT.md
@.planning/phases/01-identity-and-coaching-relationships/01-RESEARCH.md
@docs/seed/supabase_mvp1_schema_and_rls.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add invite persistence model and validation schema</name>
  <files>supabase/migrations/20260309_phase1_invites.sql, src/features/invites/schemas.ts</files>
  <action>Create migration updates needed for secure invite token lifecycle (token, expires_at, state constraints, unique resend semantics). Add zod schemas for send/resend/accept payloads and transitions.</action>
  <verify>Migration SQL passes lint/check in project workflow; schema unit tests validate accepted/rejected payloads.</verify>
  <done>Invite lifecycle has explicit persisted state and validated IO contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement trainer invite send/resend server flows</name>
  <files>src/features/invites/service.ts, src/app/api/invites/send/route.ts, src/app/api/invites/resend/route.ts, src/services/email/invite-template.ts</files>
  <action>Implement authenticated trainer-only endpoints to send and resend invite emails. Enforce single active pending invite per email and refresh invite token/expiry on resend instead of duplicating records.</action>
  <verify>API integration tests confirm send works, resend refreshes token/expiry, and duplicate pending invites are not created.</verify>
  <done>Trainer invite issuance behaves idempotently and respects one-active-invite semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Implement guided invite acceptance path</name>
  <files>src/app/invite/[token]/page.tsx, src/app/api/invites/accept/route.ts, src/features/invites/service.ts</files>
  <action>Create guided invite page and acceptance endpoint that validates token/expiry/status, supports existing or new client auth path, and atomically transitions relationship to active while preserving one-trainer-per-client constraints.</action>
  <verify>Acceptance tests cover valid token, expired token, already-used token, and one-trainer-per-client conflict behavior.</verify>
  <done>Client can only activate valid invite links and successful acceptance creates correct active relationship linkage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Invite send/resend/accept integration tests pass
- [ ] Expired invite paths are rejected with deterministic error response
- [ ] Duplicate invite creation is prevented by logic and DB constraints
</verification>

<success_criteria>

- All tasks completed
- RELA-01 and RELA-02 behaviors implemented and verifiable
- 7-day expiry and resend-refresh semantics enforced
- Relationship activation path is atomic and one-trainer-per-client safe
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-coaching-relationships/01-02-SUMMARY.md`
</output>
