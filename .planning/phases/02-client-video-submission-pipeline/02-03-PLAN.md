---
phase: 02-client-video-submission-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/app/client/submissions/page.tsx
  - src/components/submissions/SubmissionForm.tsx
  - src/components/submissions/SubmissionHistoryList.tsx
  - src/components/submissions/SubmissionStatusBadge.tsx
  - src/features/submissions/selectors.ts
  - src/features/submissions/selectors.test.ts
  - src/features/relationships/selectors.ts
autonomous: true
requirements:
  - SUBM-01
  - SUBM-02
  - SUBM-03
  - SUBM-05
user_setup: []
must_haves:
  truths:
    - "Client submissions page provides a single-submit upload form with required label and optional note."
    - "UI shows inline progress and preserves metadata for retry on failed uploads."
    - "Submission UI keeps canonical `exercise_key` while rendering the persisted display `exercise_label` in history rows."
    - "History list is newest-first, includes required row metadata/status badge, and supports explicit `Load more`."
  artifacts:
    - path: src/app/client/submissions/page.tsx
      provides: "Client submissions surface combining submission form and history list"
      min_lines: 45
    - path: src/components/submissions/SubmissionForm.tsx
      provides: "Single-submit upload form with controlled labels + Other handling"
      min_lines: 60
    - path: src/components/submissions/SubmissionHistoryList.tsx
      provides: "Newest-first history rendering and load-more behavior"
      min_lines: 50
    - path: src/features/submissions/selectors.ts
      provides: "UI state derivation for progress/error/history presentation"
      min_lines: 30
  key_links:
    - from: src/app/client/submissions/page.tsx
      to: src/app/api/submissions/create/route.ts
      via: "form submission flow"
      pattern: "create|submit|upload"
    - from: src/app/client/submissions/page.tsx
      to: src/app/api/submissions/history/route.ts
      via: "history load and load-more behavior"
      pattern: "history|load|cursor|limit"
    - from: src/features/relationships/selectors.ts
      to: src/app/client/submissions/page.tsx
      via: "existing action href route alignment"
      pattern: "client/submissions"
---

<objective>
Deliver client-facing submission UX and history experience for Phase 2.

Purpose: Complete user-visible behavior for upload, metadata entry, and owned submission tracking.
Output: `/client/submissions` page with single-submit form, inline progress/retry messaging, and newest-first history with load-more.
</objective>

<execution_context>
@C:/Users/Carboteiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Carboteiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-video-submission-pipeline/02-CONTEXT.md
@.planning/phases/02-client-video-submission-pipeline/02-RESEARCH.md
@.planning/phases/02-client-video-submission-pipeline/02-VALIDATION.md
@.planning/phases/02-client-video-submission-pipeline/02-01-SUMMARY.md
@.planning/phases/02-client-video-submission-pipeline/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build submission form components and UI state selectors</name>
  <files>src/components/submissions/SubmissionForm.tsx, src/features/submissions/selectors.ts, src/features/submissions/selectors.test.ts</files>
  <action>Implement single-submit form with searchable controlled labels, required custom text for `Other`, canonical key + display label metadata handling, optional note, and selector-driven UI states for upload progress, warning, and error/retry messaging.</action>
  <verify><automated>npm test -- src/features/submissions/selectors.test.ts</automated> Cover label validation state, in-flight state, warning state, and retry state with metadata preservation.</verify>
  <done>Submission form behavior matches locked UX decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Build history list components with status visibility and load-more</name>
  <files>src/components/submissions/SubmissionHistoryList.tsx, src/components/submissions/SubmissionStatusBadge.tsx</files>
  <action>Render newest-first history rows with submitted time, persisted display exercise label (backed by canonical key), status badge, and optional note preview. Implement explicit load-more control wired to paginated history API response.</action>
  <verify><automated>npm test -- src/components/submissions/SubmissionHistoryList.test.ts</automated> Confirm ordering, row metadata, and load-more append behavior.</verify>
  <done>Client history experience satisfies Phase 2 visibility and pagination expectations.</done>
</task>

<task type="auto">
  <name>Task 3: Compose `/client/submissions` page and align navigation route usage</name>
  <files>src/app/client/submissions/page.tsx, src/features/relationships/selectors.ts</files>
  <action>Integrate submission form + history list on submissions page, wire to create/history APIs, and confirm existing client dashboard action route remains aligned (`/client/submissions`).</action>
  <verify><automated>npm test -- src/features/relationships/selectors.test.ts src/app/api/submissions/create/route.test.ts src/app/api/submissions/history/route.test.ts</automated> Manual smoke test: submit valid video metadata, observe progress/success/error states, and verify history row appears with expected metadata/status.</verify>
  <done>End-user Phase 2 workflow is reachable and coherent from client dashboard to submission history.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Form UX verifies required label behavior, optional note, and `Other` custom text rule
- [ ] Upload progress + retry UX behaves as specified
- [ ] History list verifies newest-first ordering, key metadata rows, and load-more append
</verification>

<success_criteria>

- All tasks completed
- SUBM-01 upload flow is available and usable from client surface
- SUBM-02 and SUBM-03 metadata inputs enforced in UI + API paths
- SUBM-05 client history list and load-more behavior implemented with required row metadata
</success_criteria>

<output>
After completion, create `.planning/phases/02-client-video-submission-pipeline/02-03-SUMMARY.md`
</output>
