---
phase: 02-client-video-submission-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/features/dev/local-runtime.ts
  - src/features/dev/local-runtime.submissions.test.ts
autonomous: true
gap_closure: true
requirements:
  - SUBM-04
  - SUBM-05
user_setup: []
must_haves:
  truths:
    - "Client submission create and history flows remain available after runtime adapter hardening."
    - "Owner-scoped submission history behavior remains correct after resolving the build blocker."
    - "Phase 2 submission experience is shippable because production build and submission tests both pass."
  artifacts:
    - path: src/features/dev/local-runtime.ts
      provides: "Submission runtime adapter injection is type-safe for service and history query consumers"
      min_lines: 60
    - path: src/features/dev/local-runtime.submissions.test.ts
      provides: "Regression coverage that create/history runtime behavior still works after type fixes"
      min_lines: 25
  key_links:
    - from: src/features/dev/local-runtime.ts
      to: src/features/submissions/queries.ts
      via: "SubmissionQueries repository injection contract"
      pattern: "SubmissionQueries|repository|listByClient"
    - from: src/features/dev/local-runtime.ts
      to: src/features/submissions/service.ts
      via: "Shared runtime repository adapter used by submission service"
      pattern: "buildSubmissionRepository|createSubmissionService"
---

<objective>
Close Phase 2 BUILD-01 verification gap by fixing local runtime submission repository typing so production build passes.

Purpose: Remove the compile-time type mismatch blocking phase closure while preserving existing submission API/UI behavior.
Output: Type-safe runtime submission adapter wiring plus regression evidence (`npm run build` + submission tests).
</objective>

<execution_context>
@C:/Users/Carboteiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Carboteiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-video-submission-pipeline/02-VERIFICATION.md
@.planning/phases/02-client-video-submission-pipeline/02-02-SUMMARY.md
@.planning/phases/02-client-video-submission-pipeline/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make submission runtime adapter satisfy history-query contract</name>
  <files>src/features/dev/local-runtime.ts</files>
  <action>Refactor submission runtime adapter construction so the object injected into `SubmissionQueries` is guaranteed to implement `SubmissionHistoryRepository` (including required `listByClient`) without optional/union mismatch.</action>
  <verify><automated>npm test -- src/features/submissions/queries.test.ts src/features/dev/local-runtime.submissions.test.ts</automated> Confirm no type-level assignment errors remain in local edits.</verify>
  <done>`local-runtime` submission wiring is compile-safe for both create service and history query surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for fixed runtime wiring</name>
  <files>src/features/dev/local-runtime.submissions.test.ts</files>
  <action>Extend/adjust runtime submission tests to explicitly exercise create + history flow through the updated adapter path to guard against future contract regressions.</action>
  <verify><automated>npm test -- src/features/dev/local-runtime.submissions.test.ts src/app/api/submissions/history/route.test.ts src/app/api/submissions/create/route.test.ts</automated>.</verify>
  <done>Tests prove the runtime path still supports submission creation and owner-scoped history retrieval after type fixes.</done>
</task>

<task type="auto">
  <name>Task 3: Prove gap closure with full build and targeted submission suite</name>
  <files>src/features/dev/local-runtime.ts, src/features/dev/local-runtime.submissions.test.ts</files>
  <action>Execute full build and submission-focused tests, then record evidence in plan summary so Phase 2 verification can move from `gaps_found` to `passed`.</action>
  <verify><automated>npm test -- src/features/dev/local-runtime.submissions.test.ts src/features/submissions/queries.test.ts</automated>. Execute `npm run build` and the broader submission suite at wave gate before completion sign-off.</verify>
  <done>Build succeeds and submission feature regressions are not introduced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `src/features/dev/local-runtime.ts` no longer injects a type-incompatible repository into `SubmissionQueries`
- [ ] `npm run build` passes
- [ ] Submission route/query/runtime tests pass after the fix
</verification>

<success_criteria>

- All tasks completed
- BUILD-01 gap from `02-VERIFICATION.md` is resolved with reproducible command evidence
- Phase 2 remains stable for SUBM-04 lifecycle/state behavior and SUBM-05 history visibility
</success_criteria>

<output>
After completion, create `.planning/phases/02-client-video-submission-pipeline/02-04-SUMMARY.md`
</output>
