---
phase: 02-client-video-submission-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/submissions/schemas.ts
  - src/features/submissions/service.ts
  - src/features/submissions/schemas.test.ts
  - src/features/submissions/service.test.ts
  - src/features/dev/local-runtime.ts
autonomous: true
requirements:
  - SUBM-02
  - SUBM-03
  - SUBM-04
user_setup: []
must_haves:
  truths:
    - "Submission metadata requires a controlled exercise label, with required custom text when `Other` is selected."
    - "Submission contracts persist both canonical exercise key and display label value for downstream history/reporting."
    - "Client note remains optional and bounded by validation rules."
    - "Submission creation assigns lifecycle status deterministically, transitioning to `ready_for_review` after successful persistence."
  artifacts:
    - path: src/features/submissions/schemas.ts
      provides: "Submission payload and file-constraint validation contracts (`exercise_key` + `exercise_label`, note, type/size, status)"
      min_lines: 60
    - path: src/features/submissions/service.ts
      provides: "Submission domain service with ownership-safe creation/status rules and metadata persistence contract"
      min_lines: 80
    - path: src/features/dev/local-runtime.ts
      provides: "In-memory submission repository wiring for local/manual execution"
      min_lines: 80
  key_links:
    - from: src/features/submissions/service.ts
      to: src/features/submissions/schemas.ts
      via: "schema-validated create-submission contract"
      pattern: "parse|schema|exercise|status"
    - from: src/features/dev/local-runtime.ts
      to: src/features/submissions/service.ts
      via: "service factory registration"
      pattern: "register|submission|factory"
---

<objective>
Establish submission-domain contracts and lifecycle rules before API/UI integration.

Purpose: Lock correctness for metadata validation and status behavior so later routes/pages cannot drift.
Output: Validated submission schemas, service layer, and local runtime adapters with tests.
</objective>

<execution_context>
@C:/Users/Carboteiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Carboteiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-video-submission-pipeline/02-CONTEXT.md
@.planning/phases/02-client-video-submission-pipeline/02-RESEARCH.md
@.planning/phases/02-client-video-submission-pipeline/02-VALIDATION.md
@docs/seed/supabase_mvp1_schema_and_rls.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define submission schemas and constraints</name>
  <files>src/features/submissions/schemas.ts, src/features/submissions/schemas.test.ts</files>
  <action>Create Zod schemas for submission payloads: controlled searchable label model, explicit `exercise_key` + `exercise_label` persistence fields (required custom label on `Other`), optional client note, and file constraints (MP4/MOV, 500 MB max, soft duration warning metadata).</action>
  <verify>
    <automated>npm test -- src/features/submissions/schemas.test.ts</automated>
    Confirm rejection paths for invalid label/file payloads.
  </verify>
  <done>Submission IO contracts enforce locked metadata, including canonical key + display label persistence, and file-rule decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement submission service with deterministic status rules</name>
  <files>src/features/submissions/service.ts, src/features/submissions/service.test.ts</files>
  <action>Implement service/repository interfaces for submission creation, server-owned status assignment (`ready_for_review` after successful save), canonical key + display label metadata persistence, and duplicate/in-flight-safe create behavior expected by UI/API callers.</action>
  <verify>
    <automated>npm test -- src/features/submissions/service.test.ts</automated>
    Cover status assignment, ownership guardrails, and metadata persistence.
  </verify>
  <done>Core submission lifecycle behavior is deterministic and test-backed.</done>
</task>

<task type="auto">
  <name>Task 3: Extend local runtime with submission persistence adapter</name>
  <files>src/features/dev/local-runtime.ts, src/features/submissions/service.ts</files>
  <action>Add in-memory submission storage/query primitives and register submission service factory so Phase 2 routes/pages can execute in local runtime without external infra.</action>
  <verify>
    <automated>npm test -- src/features/dev/local-runtime.submissions.test.ts src/features/submissions/service.test.ts</automated>
    Smoke a local service call through registered runtime factories.
  </verify>
  <done>Submission domain logic is runnable in local/manual environment.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Submission schema tests pass for label/note/file rules
- [ ] Submission service tests pass for status and ownership invariants
- [ ] Local runtime registration supports submission service resolution
</verification>

<success_criteria>

- All tasks completed
- SUBM-02 and SUBM-03 metadata behavior enforced by schema/service contracts
- SUBM-04 status lifecycle baseline implemented (`ready_for_review` assignment after persistence)
- Local runtime can execute submission-domain logic without external dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/02-client-video-submission-pipeline/02-01-SUMMARY.md`
</output>
