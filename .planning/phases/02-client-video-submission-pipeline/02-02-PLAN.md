---
phase: 02-client-video-submission-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/api/submissions/create/route.ts
  - src/app/api/submissions/history/route.ts
  - src/features/submissions/service.ts
  - src/features/submissions/queries.ts
  - src/features/submissions/queries.test.ts
  - src/features/dev/local-runtime.ts
autonomous: true
requirements:
  - SUBM-01
  - SUBM-04
  - SUBM-05
user_setup: []
must_haves:
  truths:
    - "Client can submit a video payload through authenticated API flow from mobile or desktop."
    - "Submission API persists and returns both canonical `exercise_key` and display `exercise_label` metadata values."
    - "Server enforces file and metadata constraints and does not trust client-supplied lifecycle status."
    - "Client history API returns newest-first rows for authenticated owner only, with load-more pagination shape."
  artifacts:
    - path: src/app/api/submissions/create/route.ts
      provides: "Authenticated submission-create endpoint with multipart/form-compatible payload handling"
      exports: ["POST"]
    - path: src/app/api/submissions/history/route.ts
      provides: "Client-owned paginated history endpoint"
      exports: ["GET"]
    - path: src/features/submissions/queries.ts
      provides: "History query layer with ownership and cursor/limit semantics"
      min_lines: 50
  key_links:
    - from: src/app/api/submissions/create/route.ts
      to: src/features/submissions/service.ts
      via: "submission creation orchestration"
      pattern: "create|status|file|client"
    - from: src/app/api/submissions/history/route.ts
      to: src/features/submissions/queries.ts
      via: "history fetch with pagination"
      pattern: "history|limit|cursor|client"
    - from: src/features/submissions/queries.ts
      to: src/features/dev/local-runtime.ts
      via: "read adapter implementation for local runtime"
      pattern: "list|owner|submission"
---

<objective>
Implement submission API surfaces and history query contracts on top of Phase 2 domain primitives.

Purpose: Make submission creation/history executable through stable HTTP interfaces for UI consumption.
Output: Create + history endpoints with ownership guarantees, deterministic status behavior, and pagination-ready response shape.
</objective>

<execution_context>
@C:/Users/Carboteiro/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Carboteiro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-video-submission-pipeline/02-CONTEXT.md
@.planning/phases/02-client-video-submission-pipeline/02-RESEARCH.md
@.planning/phases/02-client-video-submission-pipeline/02-VALIDATION.md
@.planning/phases/02-client-video-submission-pipeline/02-01-SUMMARY.md
@docs/seed/supabase_mvp1_schema_and_rls.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build authenticated submission-create API</name>
  <files>src/app/api/submissions/create/route.ts, src/features/submissions/service.ts</files>
  <action>Implement client-authenticated POST route that accepts file + metadata payload, applies server-side validation, delegates to submission service, and persists/returns canonical `exercise_key` with display `exercise_label` in deterministic success/error envelopes for UI progress/retry behavior.</action>
  <verify>
    <automated>npm test -- src/app/api/submissions/create/route.test.ts</automated>
    Cover valid submit, invalid file type, oversize file, missing required custom label, and unauthenticated/forbidden cases.
  </verify>
  <done>Create route supports single-submit workflow with authoritative server constraints and explicit key+label metadata contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement client-owned history query layer and API</name>
  <files>src/features/submissions/queries.ts, src/features/submissions/queries.test.ts, src/app/api/submissions/history/route.ts</files>
  <action>Create newest-first history query with limit/cursor contract and expose it through a client-authenticated GET endpoint that guarantees owner-only visibility and returns persisted `exercise_key` + `exercise_label` fields.</action>
  <verify>
    <automated>npm test -- src/features/submissions/queries.test.ts src/app/api/submissions/history/route.test.ts</automated>
    Confirm ownership and pagination behavior.
  </verify>
  <done>History API supports load-more UX without cross-client leakage while preserving canonical key + display label metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Wire local runtime adapters for create/history endpoints</name>
  <files>src/features/dev/local-runtime.ts, src/features/submissions/service.ts, src/features/submissions/queries.ts</files>
  <action>Extend local runtime state/adapters so submission create/history APIs are runnable in dev without external storage/db dependencies.</action>
  <verify>
    <automated>npm test -- src/features/dev/local-runtime.submissions.test.ts src/app/api/submissions/create/route.test.ts src/app/api/submissions/history/route.test.ts</automated>
    Confirm create and history endpoints function in local runtime.
  </verify>
  <done>Phase 2 backend surfaces run end-to-end in current local environment.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Submission-create API rejects invalid file/metadata and accepts valid payload
- [ ] History API is owner-scoped and newest-first with load-more response shape
- [ ] Local runtime supports both routes without service-not-configured errors
</verification>

<success_criteria>

- All tasks completed
- SUBM-01 upload submission path is available via authenticated API
- SUBM-04 status persistence contract enforced through service/route integration
- SUBM-05 history retrieval contract implemented with owner-only visibility
</success_criteria>

<output>
After completion, create `.planning/phases/02-client-video-submission-pipeline/02-02-SUMMARY.md`
</output>
